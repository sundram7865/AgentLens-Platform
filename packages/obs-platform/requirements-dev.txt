-r requirements.txt

# The default suite runs with no containers: SQLite stands in for Postgres and
# fakeredis for Redis, which keeps the inner loop fast and CI free. The
# integration and end-to-end layers use the same packages against real services
# (OBS_INTEGRATION=1 / OBS_E2E=1); they need no extra dependencies, only Docker.
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
fakeredis==2.37.1
aiosqlite==0.22.1

ruff==0.13.3
mypy==1.18.2
pip-audit==2.9.0
